import { LightningElement } from 'lwc';

export default class Paytransation extends LightningElement {
    // // @track data = [];
    // recordId;
    // recordPageUrl;
    // wiredActivities;
    
    // // @wire(getRecords)
    // // wiredclass({error,data}){
    // //   // this.wiredActivities = value;
    // //   // const { data, error } = value;
    // //   if (data) { 
    // //     // let lstdata = JSON.parse(JSON.stringify(data));
    // //     console.log("data:" +data);
    // //       this.data = data;
    // //       this.error = undefined;  
    // //   } else if (error) {  
    // //       this.error = error; 
    // //       this.data = undefined;  
    // //   }  
}